# AI 视频提示词工作台（展示原型）

此目录保留两个可直接在浏览器打开的前端展示原型：

- `video-prompt-workbench.html`：提示词拆解、工作流选择和交互面板。
- `video-pipeline-prototype.html`：视频生成流程编排的界面原型。

它们用于呈现产品设计与 Vibe Coding 能力，不连接任何生产 API，也不包含账号、令牌、Cookie 或内部自动化逻辑。

## 预览

从仓库根目录运行静态服务器后，访问对应 HTML 文件即可。例如：

```bash
python3 -m http.server 8080
```
